# Test Environment Variables
# Used by GitHub Actions and local test runs

NODE_ENV=test
PORT=3000
BASE_URL=http://localhost:3000

# JWT Secret (test value only - not for production)
JWT_SECRET=test_jwt_secret_for_ci_cd_only_not_production_use_minimum_32_chars

# Database (in-memory for tests)
DB_PATH=:memory:
